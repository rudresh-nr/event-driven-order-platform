global:
  scrape_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "django"
    static_configs:
      - targets:
          - web:8000
  
  - job_name: "node"
    static_configs:
      - targets:
          - node-exporter:9100